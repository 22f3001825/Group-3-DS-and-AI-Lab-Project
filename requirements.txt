# CS2007 MLT RAG Pipeline & AI Learning Assistant — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Core RAG & LLM Framework ──────────────────────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-text-splitters
langchain-community
langchain-google-genai
langchain-groq
groq>=0.9.0
langchain-openai        # only for LLM_PROVIDER=local (any OpenAI-compatible endpoint)

# ── Tokenization & Embeddings ──────────────────────────────────────────────────
tiktoken
fastembed>=0.3.0

# ── Text Extraction & Document Parsing ────────────────────────────────────────
pymupdf
pymupdf4llm
beautifulsoup4
markdownify
markdown
easyocr

# ── Vector Database ────────────────────────────────────────────────────────────
qdrant-client>=1.9.0
langchain-qdrant

# ── Backend API & Database ────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart        # required by FastAPI for UploadFile (admin PDF upload)
sqlalchemy>=2.0.0
pydantic>=2.6.0
python-dotenv>=1.0.0

# ── Authentication ────────────────────────────────────────────────────────────
# Google Sign-In is the only way into the app; both of these are load-bearing.
PyJWT>=2.8.0            # HS256 session tokens issued by /auth/google
google-auth>=2.28.0     # verify_oauth2_token: JWKS fetch/cache, signature, iss, aud, exp
                        #   (already importable via langchain-google-genai, but undeclared —
                        #    a transitive bump could drop it)

# ── Data Processing & Analytics ────────────────────────────────────────────────
pandas
numpy
tqdm
requests
